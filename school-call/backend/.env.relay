# ────────────────────────────────────────────────────────
# relay 모드 환경변수 (로컬 단독 운영 또는 Cloud VPS 배포 시 사용.)
# 실행: docker compose --env-file .env.relay up -d
# ────────────────────────────────────────────────────────

MODE=relay
NODE_OPTIONS=--max-old-space-size=512

# 접속을 허용할 외부 도메인 (쉼표 구분)
ALLOWED_ORIGINS=https://luminousky.com,https://www.luminousky.com

# 신호를 복제할 외부 relay 서버 주소
# relay 모드에서는 사용하지 않음
CLOUD_URL=

# ── Server-to-Server Authentication Key ──
# 해당 키가 일치하는 Mirror 서버는 도메인(Origin) 검증을 우회하여 Relay 서버와 연동됩니다.
MIRROR_SECRET_KEY=super_secret_password_123!
